extends layout.pug

block principal

    section(class="login")
      h1 Login
      form(action="/login", method="POST")
        input(type="text", name="username", id="username", required = true, placeholder="Username", value=username)
        br
        input(type="password", name="password", id="password", required = true, placeholder="Password")
        br
        br
        if error
          p(class="error-messages")= error
        br
        button(type="submit") Login
        button(type="button", onclick="/signin") Sign in
